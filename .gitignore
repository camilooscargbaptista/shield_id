__pycache__/
*.py[cod]
.venv/
venv/
.env
.env.local
*.egg-info/
.pytest_cache/
.ruff_cache/
.mypy_cache/
dist/
build/
# Agent runtime state: working copy is ephemeral; truth lives in PRs + CI + approval-log.jsonl
.agent/state/current-experiment.json
# Data & model artifacts never committed (no raw biometrics, no real PII — rules 03/04)
data/raw/
data/biometric/
# Generated datasets (e.g. RAID→JSONL loader output) — synthetic/derived, never versioned
data/*.jsonl
data/*.csv
*.ckpt
*.pt
*.onnx
