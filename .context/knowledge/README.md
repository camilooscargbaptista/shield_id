# .context/knowledge — Per-domain wiki (with teeth)

The wiki that gets maintained because the delivery gate forces it. Read the whole `<domain>.md` before
touching a domain; introduce a new pattern → update it in the SAME PR; contradict one → ADR + update.
Files: layer1-detection · layer2-behavioral · aita-policy · fairness.
